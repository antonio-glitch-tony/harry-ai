services:
  - type: web
    name: jarvis-ai
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.19.6
      - key: PORT
        value: 3000
    healthCheckPath: /health
    autoDeploy: true